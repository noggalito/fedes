{
  deploy : {
    production : {
      user : "macool",
      host : "produccion.fedes.ec",
      ref  : "origin/master",
      repo : "git@github.com:noggalito/fedes.git",
      path : "~/fedes",
      "post-deploy" : "export PATH=~/.nvm/versions/v0.12.14/bin:$PATH && npm install --production && pm2 restart fedes"
    }
  }
}
